@model Utente
@{
    ViewData["Title"] = "Logout";
}

<div class="d-flex justify-content-center align-items-center vh-100">
    <div class="text-center">
        <h1 class="display-4">Logout</h1>
        <p class="lead">Per effettuare il logout clicca sul bottone</p>
        <form asp-controller="Home" asp-action="P2" method="post">
            <button type="submit" class="btn btn-danger btn-lg">Logout</button>
        </form>
        <p class="mt-3"><a class="btn btn-primary" href=http://localhost:5294/Home/Eventi>Ritorna agli Eventi</a></p>
    </div>
</div>